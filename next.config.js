/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost"], // Add any other domains you need here
  },
};

module.exports = nextConfig;
